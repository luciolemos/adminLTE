{# Partials/_dashboard_header.twig #}
<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1><i class="{{ icon|default('fas fa-tachometer-alt') }}"></i> {{ title|default('Dashboard') }}</h1>
      </div>
      <div class="col-sm-6">
        {% include 'Partials/_breadcrumb.twig' %}
      </div>
    </div>
  </div>
</section>
